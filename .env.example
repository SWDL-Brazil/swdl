# =============================================================
#  SWDL — Variáveis de Ambente (copie para .env)
# =============================================================

# Obrigatório — used para assinatura de certificados e sessões
SECRET_KEY=

# Opcional — senha do admin padrão (fallback: swdl2025)
ADMIN_PASSWORD=

# Opcional — origins permitidas no CORS (fallback: *)
# Exemplo: https://swdl-5a3fa.web.app,https://localhost:5000
CORS_ORIGINS=
